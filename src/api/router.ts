import axios from 'axios'

async function getNavigationItems (): Promise < void > {
  /*
  const response = await axios.request <[IMenu<IMenuItem>]>({
    url: '/api/view/menu/all',
    method: 'GET'
  })
  const { data } = response
  return data || null
  */
}

export {
  getNavigationItems
}
